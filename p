"""
# Django_startup- where "FirstProjectSetting" is a project ( 4.2 long tearm project )

Created FOLDER in Desktop open in VSCOD
Command : virtualenv .venv
Created : touch requirements.tx (NewFile)
        : Inside of the requiremts.txt file django==4.2.2        
command : source .venv/scripts/activate (for window)
        : source .vend/bin/activate (for mac)
        :  pip install -r requirements.txt               
command : pip freeze   (to see the requirements file)
command : pip freeze > requirements.txt
command : pip install -r requirements.txt
command : django-admin --version
command : django-admin startproject abc . ( project name  abc with DOT to creat write the  path)
command : python manage.py runserver
 
"""
